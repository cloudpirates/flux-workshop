apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - name: http
      port: 9898
      targetPort: http
    - name: grpc
      port: 9999
      targetPort: grpc
